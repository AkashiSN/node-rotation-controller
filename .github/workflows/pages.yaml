name: Pages
on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      # copy-readme.mjs generates the Getting Started pages from these at build
      # time, so a README-only change must also trigger a redeploy.
      - 'README.md'
      - 'README.ja.md'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/pages.yaml'
      # The simulator page (issue #240) serves a wasm module built from these — a
      # change to internal/sim or cmd/wasm changes the page's behaviour, so it must
      # redeploy.
      - 'cmd/wasm/**'
      - 'internal/**'
      - 'api/**'
      - 'go.mod'
      - 'go.sum'
      - 'Makefile'
      - 'aqua.yaml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: aquaproj/aqua-installer@v4.0.4
        with:
          aqua_version: v2.60.1
          policy_allow: "true"
      - name: Cache Go + aqua tools
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            ~/.local/share/aquaproj-aqua
          key: pages-${{ runner.os }}-${{ hashFiles('go.sum', 'aqua.yaml') }}
          restore-keys: pages-${{ runner.os }}-
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: npm ci
      - run: npm run docs:build
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: docs/.vitepress/dist
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
