# Patterns to ignore when building Helm packages.
.git/
.gitignore
*.tmproj
.vscode/
.idea/
*.bak
*.swp
*~
# helm-unittest fixtures (issue #75) — assert rendered RBAC/PriorityClass and
# schema rejections in CI; not part of the released chart package.
tests/
