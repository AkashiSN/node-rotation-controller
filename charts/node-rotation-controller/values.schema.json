{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "node-rotation-controller values",
  "description": "Install-time validation of the chart values. The CRD's OpenAPI schema and the controller re-validate the rendered RotationPolicy at admission/reconcile time (spec §5.4) — this schema is a fast first line of defense, not the source of truth.",
  "type": "object",
  "properties": {
    "config": {
      "not": {},
      "description": "Removed in #119: policy moved from the `config` ConfigMap block to the RotationPolicy CRD (see `rotationPolicy` below). Rejected so a pre-#119 upgrade carrying legacy `config.policy.*` values fails loudly at render instead of silently ignoring the user's policy."
    },
    "replicaCount": {
      "type": "integer",
      "minimum": 1,
      "description": "v1 expects 2 for leader election (spec §5.1)."
    },
    "image": {
      "type": "object",
      "properties": {
        "repository": { "type": "string", "minLength": 1 },
        "pullPolicy": { "type": "string", "enum": ["Always", "IfNotPresent", "Never"] },
        "tag": { "type": "string" }
      }
    },
    "placeholder": {
      "type": "object",
      "properties": {
        "image": { "type": "string", "minLength": 1 },
        "priorityClass": {
          "type": "object",
          "properties": {
            "create": { "type": "boolean" },
            "name": { "type": "string", "minLength": 1 },
            "value": {
              "type": "integer",
              "maximum": -1,
              "description": "Must be negative so the placeholder is the preemption victim (spec §3.3)."
            }
          }
        }
      }
    },
    "prometheusRule": {
      "type": "object",
      "properties": {
        "enabled": { "type": "boolean" },
        "labels": { "type": "object", "additionalProperties": { "type": "string" } },
        "completedFailure": { "$ref": "#/definitions/alertTuning" },
        "candidatesNotDraining": {
          "allOf": [
            { "$ref": "#/definitions/alertTuning" },
            { "properties": { "windowRange": { "$ref": "#/definitions/promDuration" } } }
          ]
        },
        "stalledInWindow": {
          "allOf": [
            { "$ref": "#/definitions/alertTuning" },
            { "properties": { "completionRange": { "$ref": "#/definitions/promDuration" } } }
          ]
        },
        "drainStuck": { "$ref": "#/definitions/alertTuning" },
        "shortLeadNodes": { "$ref": "#/definitions/alertTuning" },
        "retryCountHigh": { "$ref": "#/definitions/alertTuning" }
      }
    },
    "podDisruptionBudget": {
      "type": "object",
      "properties": {
        "enabled": { "type": "boolean" },
        "minAvailable": { "type": ["integer", "string"] }
      }
    },
    "rotationPolicies": {
      "type": "array",
      "description": "Multiple RotationPolicy objects (issue #153), one per entry. When non-empty it supersedes the singular `rotationPolicy` below. Each entry's `spec` is the same shape as `rotationPolicy.spec`.",
      "items": {
        "type": "object",
        "required": ["name", "spec"],
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "Cluster-scoped RotationPolicy name; required and must be unique across entries."
          },
          "create": {
            "type": "boolean",
            "description": "Set false to skip just this entry (defaults true)."
          },
          "spec": { "$ref": "#/definitions/rotationPolicySpec" }
        }
      }
    },
    "rotationPolicy": {
      "type": "object",
      "deprecated": true,
      "description": "DEPRECATED (issue #153): superseded by `rotationPolicies` and slated for removal at the 1.0 milestone. Still rendered (and the default) while `rotationPolicies` is empty; a single-entry `rotationPolicies` list is the one-to-one replacement.",
      "properties": {
        "create": { "type": "boolean" },
        "name": { "type": "string" },
        "spec": { "$ref": "#/definitions/rotationPolicySpec" }
      }
    }
  },
  "definitions": {
    "rotationPolicySpec": {
      "type": "object",
      "required": ["nodePoolSelector", "maintenanceWindows"],
      "additionalProperties": false,
      "properties": {
            "nodePoolSelector": {
              "type": "object",
              "minProperties": 1,
              "additionalProperties": false,
              "properties": {
                "matchLabels": {
                  "type": "object",
                  "minProperties": 1,
                  "additionalProperties": { "type": "string" }
                },
                "matchExpressions": {
                  "type": "array",
                  "minItems": 1,
                  "items": {
                    "type": "object",
                    "required": ["key", "operator"],
                    "properties": {
                      "key": { "type": "string", "minLength": 1 },
                      "operator": {
                        "type": "string",
                        "enum": ["In", "NotIn", "Exists", "DoesNotExist"]
                      },
                      "values": { "type": "array", "items": { "type": "string" } }
                    }
                  }
                }
              }
            },
            "ageThreshold": {
              "type": "string",
              "description": "\"auto\" or a Go duration (e.g. \"120h\")."
            },
            "minRotationChances": { "type": "integer", "minimum": 1 },
            "maintenanceWindows": {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "object",
                "required": ["timezone", "days", "start", "end"],
                "properties": {
                  "timezone": { "type": "string", "minLength": 1 },
                  "days": {
                    "type": "array",
                    "minItems": 1,
                    "items": {
                      "type": "string",
                      "enum": ["Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"]
                    }
                  },
                  "start": { "type": "string", "pattern": "^([01][0-9]|2[0-3]):[0-5][0-9]$" },
                  "end": { "type": "string", "pattern": "^([01][0-9]|2[0-3]):[0-5][0-9]$" }
                }
              }
            },
            "surge": {
              "type": "object",
              "properties": {
                "maxUnavailable": {
                  "const": 1,
                  "description": "v1 is surge-only and serial per NodePool (spec §5.4)."
                },
                "readyTimeout": { "type": "string", "pattern": "^([0-9]+(\\.[0-9]+)?(ns|us|ms|s|m|h))*([0-9]*[1-9][0-9]*(\\.[0-9]+)?|[0-9]+\\.[0-9]*[1-9][0-9]*)(ns|us|ms|s|m|h)([0-9]+(\\.[0-9]+)?(ns|us|ms|s|m|h))*$" },
                "cooldownAfter": { "type": "string", "pattern": "^([0-9]+(\\.[0-9]+)?(ns|us|ms|s|m|h))*([0-9]*[1-9][0-9]*(\\.[0-9]+)?|[0-9]+\\.[0-9]*[1-9][0-9]*)(ns|us|ms|s|m|h)([0-9]+(\\.[0-9]+)?(ns|us|ms|s|m|h))*$" },
                "retryBackoff": { "type": "string", "pattern": "^([0-9]+(\\.[0-9]+)?(ns|us|ms|s|m|h))*([0-9]*[1-9][0-9]*(\\.[0-9]+)?|[0-9]+\\.[0-9]*[1-9][0-9]*)(ns|us|ms|s|m|h)([0-9]+(\\.[0-9]+)?(ns|us|ms|s|m|h))*$" },
                "matchNodeRequirements": {
                  "type": "object",
                  "properties": {
                    "required": { "type": "array", "items": { "type": "string" } },
                    "preferred": { "type": "array", "items": { "type": "string" } }
                  }
                }
              }
            },
            "prePull": {
              "type": "object",
              "properties": {
                "enabled": {
                  "const": false,
                  "description": "v2 feature; must be false in v1 (spec §5.4)."
                }
              }
            }
          }
    },
    "promDuration": {
      "type": "string",
      "pattern": "^([0-9]+(ms|s|m|h|d|w|y))+$",
      "description": "A Prometheus range/duration string (e.g. \"4h\", \"8d\")."
    },
    "alertTuning": {
      "type": "object",
      "properties": {
        "for": { "$ref": "#/definitions/promDuration" },
        "severity": { "type": "string", "minLength": 1 }
      }
    }
  }
}
